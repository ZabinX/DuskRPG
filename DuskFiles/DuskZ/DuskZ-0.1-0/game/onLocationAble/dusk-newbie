
// TODO: This could give a directional message based on location/range
// could be angle + distance based
if (trigger.isPlayer() && trigger.getInteger("dusk-tutorial") == 0) {
    trigger.chat("You need to visit the Newbie Tutorial first, south of Eduardo villiage.");
    // if following?
    //trigger.leave();
    false;
} else {
    true;
}
