trigger mptoken trigger mp trigger maxmp trigger mp trigger maxmp *trigger "You are not in need of the MP pool." mpdiff - trigger maxmp trigger mp cost / mpdiff 2 trigger cash cost ,trigger * -1 cost -trigger trigger maxmp *trigger "As you step into the bath, your MP is restored to full." !trigger mptoken mpafford * trigger cash 2 -trigger 0 ,trigger mpafford *trigger concat concat "You could only afford "mpafford " MP." !trigger mptoken  