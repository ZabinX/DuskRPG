#
if isInBattle trigger
	livingthing target enemy trigger
	number damage * trigger stre + 4 * rand 0.5
	inc target hp * -1 damage
	inc trigger damdone damage
	chat trigger concat concat concat concat concat concat "***	" name trigger " DID " damage " TO " name target " WITH A FIRE ATTACK"
	chat target concat concat concat concat concat concat "***	" name trigger " DID " damage " TO " name target " WITH A FIRE ATTACK"
	end
#
endscript



